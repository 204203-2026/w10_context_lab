# w10_context_lab - .env is the DEMO TARGET of Step 1, not a real config file.
#
# Step 1 teaches: your agent must be DENIED reading the real secret file.
# This lab ships only a decoy value, so nothing real can leak. Do not put
# a real key here, and do not commit .env (it is gitignored).
DECOY_API_KEY=sk-FAKE-000000000000000000000000
